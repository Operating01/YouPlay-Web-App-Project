<aside class="sidebar">
    <h5 class="mb-3">Playlists</h5>
    <div class="playlist-search mb-3">
    <input type="text" id="PlaylistSearch" class="form-control" placeholder="Search playlists...">
    <div id="playlistSearchResults" class="mt-2"></div>
    </div>
    <div class="playlist-list">
        <div class="list-group">
        </div>
        <button class="btn btn-sm btn-dark mt-3 w-100" id="newPlaylistBtn">
            <i class="bi bi-plus-circle me-1"></i> New Playlist
        </button>
    </div>
</aside>


<div class="modal fade" id="selectPlaylistModal" tabindex="-1">
  <div class="modal-dialog modal-dialog-scrollable">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Select a Playlist</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      
      <div class="modal-body">
        <div id="playlistList" class="d-flex flex-column gap-2"></div>
      </div>
    </div>
  </div>
</div>


<style>
    .sidebar {
        width: 250px;
        background-color: rgba(255, 255, 153, 0.85);
        padding: 20px;
        overflow-y: auto;
        position: fixed;
        left: 0;
        top: 56px;
        bottom: 0;
        z-index: 1;
        box-shadow: 2px 0 5px rgba(0,0,0,0.1);
    }
    .playlist-item {
        background-color: transparent !important;
        border: 1px solid rgba(0,0,0,0.1) !important;
        margin-bottom: 8px;
        transition: all 0.2s;
    }
    .playlist-item:hover {
        background-color: rgba(255, 255, 255, 0.5) !important;
        transform: translateX(5px);
    }
</style>